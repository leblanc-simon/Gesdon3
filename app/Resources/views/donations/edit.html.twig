{% extends 'base.html.twig' %}
{% trans_default_domain 'edit' %}

{% block body %}
    <ol class="breadcrumb">
        <li><a href="{{ path('homepage') }}">{{ 'homepage'|trans({}, 'breadcrumb') }}</a></li>
        <li><a href="{{ path('donations') }}">{{ 'donation.list'|trans({}, 'breadcrumb') }}</a></li>
        <li class="active">{{ 'donation.edit'|trans({}, 'breadcrumb') }}</li>
    </ol>

    <h1>{{ 'edit.donation'|trans({'%name%': donation.uuid}) }}</h1>

    {% include 'donations/_form.html.twig' %}
{% endblock %}